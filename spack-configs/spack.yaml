spack:
  concretizer:
    reuse: true
    unify: true
  view: false
  packages:
    all:
      # Use a specific version of GCC
      # require: "%gcc@11.2.0"
       require: "%gcc@13.2.1"
  config:
    connect_timeout: 100
    build_stage:
      - $spack/../spack-build
  include:
    - $spack/../spack-configs/rocm-6.2.0
    # - $spack/../spack-configs/e4s-configs/frontier/rocm-5.3.0
